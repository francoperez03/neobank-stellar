{
  "name": "@neobank-stellar/api",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "dev": "drizzle-kit push && bun run --hot src/index.ts",
    "start": "bun run src/index.ts",
    "typecheck": "tsc --noEmit",
    "lint": "echo 'no lint configured for apps/api yet'",
    "db:push": "drizzle-kit push",
    "db:studio": "drizzle-kit studio"
  },
  "dependencies": {
    "hono": "^4.12.27",
    "pino": "^10.3.1",
    "drizzle-orm": "^0.36.0",
    "postgres": "^3.4.5"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "pino-pretty": "^13.1.3",
    "typescript": "^5.4.0",
    "drizzle-kit": "^0.28.0"
  }
}