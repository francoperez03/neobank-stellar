{
  "name": "@neobank-stellar/api",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "dev": "bun run --hot src/index.ts",
    "start": "bun run src/index.ts",
    "typecheck": "tsc --noEmit",
    "lint": "echo 'no lint configured for apps/api yet'"
  },
  "dependencies": {
    "hono": "^4.12.27",
    "hono-rate-limiter": "^0.5.3",
    "pino": "^10.3.1"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "pino-pretty": "^13.1.3",
    "typescript": "^5.4.0"
  }
}